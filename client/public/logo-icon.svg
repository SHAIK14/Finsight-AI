<svg width="40" height="40" viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Minimal insight lens mark -->
  <!-- Outer subtle ring -->
  <circle cx="20" cy="20" r="16" stroke="currentColor" stroke-width="1" fill="none" opacity="0.15"/>
  
  <!-- Main ring -->
  <circle cx="20" cy="20" r="11" stroke="currentColor" stroke-width="1.5" fill="none" opacity="0.4"/>
  
  <!-- Core focus circle -->
  <circle cx="20" cy="20" r="5" stroke="currentColor" stroke-width="1.5" fill="none"/>
  
  <!-- Insight spark - the "aha" moment -->
  <path d="M28 12 L33 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
  <circle cx="35" cy="5" r="2" fill="currentColor"/>
</svg>
